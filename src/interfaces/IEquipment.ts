export interface IEquipment {
    patrimonio: string,
    sala: string,
    categoria: string,
    situacao: string,
    item: number | string | null,
    nome: string,
    lotacao: string | null,
    datacriacao: string
}